# Statistics and Data Analysis

## Requirements

- Python 3.11 or newer

### Installation

The required Python dependencies can be installed with the following command:

```bash
pip install -r requirements.txt
```

The scripts are run individually from this directory:

```bash
python data_extraction/extract_articles.py
```

The required input files and output directories are defined in the individual
scripts.
